services:
  celery-app:
    build: ../celery-app/
    environment:
      - DOCKER_TESTS=1
    networks:
      - test_network

  redis-server:
    image: redis
    ports:
      - 40001:6379
    networks:
      - test_network

networks:
  test_network:
